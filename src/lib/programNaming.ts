/** Canonical user-facing program name (P5-T1). */
export const EMCI_PROGRAM_NAME = 'Enhanced My Career Insights (Pilot Program)';

/** Branded line: acronym plus full program name. */
export const EMCI_BRAND = `EMCI — ${EMCI_PROGRAM_NAME}`;

/** References to the product in auth, onboarding, and system messages. */
export const EMCI_PLATFORM = `${EMCI_BRAND} platform`;

/** Contact phrase for access and invite issues. */
export const EMCI_PLATFORM_ADMINISTRATOR = `${EMCI_BRAND} platform administrator`;
